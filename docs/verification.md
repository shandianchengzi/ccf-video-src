# 验证记录

## 2026-09-13 实际运行结果

[运行 34767785863](https://github.com/shandianchengzi/ccf-video-src/actions/runs/34767785863)，实现提交 `d358f7915dbbbd0c449315fb89c926bd1bc340bb`：

- build 成功：7 项 Python 测试、32 项 Java 契约检查通过；Android SDK 编译和 D8 生成成功。
- 下载检查 dex，包含 14 个插件实现类，未包含编译用 Spider 桩类。
- catalog 成功：原站报告 10083 条，去重后 10082 条；50 个系列完整同步。
- 下载检查最终 9 文件发布包：ZIP SHA-256、清单中各文件 SHA-256、JAR MD5、目录 schema 和 type-3 订阅配置全部通过。
- 真实接口搜索“量子”返回 221 条；“2021-2015 + CNCC”返回 1001 条；两者第一页浏览数为数值降序。
- 匿名免费视频 `8194888705853440` 的 Range 请求返回 HTTP 206、video/mp4，读取前 1024 字节确认 MP4 ftyp 标记；这不是影视仓实机测试。
- 官方登录入口跳转到 HTTPS `passport.ccf.org.cn/sso/login`。未登录用户账号。
- **deploy 暂未成功**：configure-pages 返回 Not Found，提示启用 Pages 并选择 GitHub Actions 来源。需在仓库 Settings → Pages 完成首次启用，然后在上述运行中选择 Re-run failed jobs。产物已保存在运行附件中，无需重新抓取。
- 云浏览器无法访问本地预览地址，因此网页视觉与交互尚未验收；Pages 发布后可继续。

| 研究方向 | 多标签分类条数 |
| --- | ---: |
| 嵌入式与固件仿真 | 59 |
| 芯片与数字电路 | 538 |
| AI应用与边缘智能 | 1662 |
| 大模型与智能体 | 1293 |
| 具身智能与机器人 | 370 |
| 复杂工程与系统方法 | 408 |
| 同态加密与隐私计算 | 62 |
| 量子计算与量子信息 | 313 |

条数可重叠，并随每日同步变化。

## 已在开发环境核对

- CCF 筛选、列表和详情接口真实可访问；已比较免费与会员条目的权限响应。
- Python 测试涵盖服务器分页大小限制、重复页、异常空页、历史 ID 尾部空格、大整数 ID、历史年份、多分类、英文缩写边界及公共字段白名单。
- 仓库中不存在 CCF 账号密码或实际登录 Cookie。匿名调试详情留在临时环境，不提交。

## 由 GitHub Actions 执行

- Java 契约测试：允许与拒绝代码、购买覆盖、未知权限拒绝、恶意域名边界、媒体地址参数、历史年份、分页和排序。
- 真实 Android SDK 编译、D8 转 dex；产物不得包含编译期 Spider 实现。
- 全量公开列表与系列抓取、白名单发布检查、目录回退保护、Pages 部署。
- 实际结果以 [Actions](https://github.com/shandianchengzi/ccf-video-src/actions) 中对应提交的任务状态为准。

## 目标设备待验收

没有收到用户具体影视仓版本、Android 设备或已登录 CCF 会话，不能声称以下项目已实测通过：

- 导入 tvbox.json 并加载 csp_CCF。
- 原站年份 / 系列 / 排序筛选和搜索分页。
- 账号窗口打开、官方登录 / 验证码交互、Cookie 保存。
- 免费视频播放；会员本人有权限的视频播放；无权限视频明确拒绝。
- 四档清晰度切换、拖动进度、登录失效后的重新登录。

若反馈问题，请提供影视仓版本、Android 版本、操作步骤和错误提示，可附公开视频编号。不要提供密码、Cookie 或带鉴权参数的媒体 URL。
